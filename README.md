# Gamma Controller for Rust

Программа для настройки гаммы монитора в реальном времени, специально разработанная для игры **Rust**. Позволяет улучшить видимость в ночное время и в теневых областях игры.

## 🎯 Возможности

- **Регулировка гаммы** в диапазоне от `0.1` до `4.4`
- **Два цветовых режима**:
  - `all` - применяет гамму ко всем цветовым каналам
  - `blue` - применяет гамму только к синему каналу
- **Горячие клавиши** для быстрого управления
- **Автосохранение настроек** между сеансами
- **Автоматическое восстановление** стандартной гаммы при выходе

## ⌨️ Управление

| Клавиша | Действие |
|---------|----------|
| **F2** | Включить/выключить гамма-коррекцию |
| **F3** | Увеличить гамму (+0.1) |
| **F4** | Уменьшить гамму (-0.1) |
| **F5** | Переключить цветовой режим |

## 🛠️ Требования и совместимость

### ✅ Поддерживаемые системы
- **Только Windows** (использует WinAPI для управления гаммой)

### ❌ Не поддерживается
- **Linux** и другие ОС (код использует Windows-specific API)

### 📋 Зависимости
- Python 3.6+
- Библиотеки: `ctypes` (входит в стандартную библиотеку Python)

## 🚀 Установка и запуск

1. Скачайте файл `gamma.py`
2. Убедитесь, что у вас установлен Python 3.6+
3. Запустите командой:
   ```cmd
   python gamma.py
   ```

## ⚙️ Конфигурация

Настройки автоматически сохраняются в файл `gamma_config.json` на рабочем столе. Можно редактировать вручную:

```json
{
    "gamma": 2.0,
    "color_mode": "all",
    "hotkeys": {
        "toggle": "F2",
        "increase": "F3", 
        "decrease": "F4",
        "cycle_color": "F5"
    }
}
```

## 🎮 Использование в Rust

1. Программу можно запускать как до, так и после запуска Rust.
2. Используйте горячие клавиши для настройки.
3. Для выхода закройте окно/

## ⚠️ Важно

- Программа работает на уровне системы и влияет на все приложения
- После закрытия программы гамма не сбрасывается.
- Рекомендуется использовать режим `"all"` для сохранения цветового баланса
- Не превышайте гамму `3.0` для комфортного использования

## 🔧 Технические детали

Использует WinAPI функции `SetDeviceGammaRamp` для аппаратной коррекции гаммы. Работает со всеми активными мониторами в системе.

---

> **Примечание**: Программа предназначена для легального использования в играх, где разрешена настройка гаммы. Используйте на свой страх и риск.
